<nav class="bg-blue-600 text-white sticky top-0 z-50">
    <div class="container mx-auto px-4">
        <div class="flex items-center justify-between h-14">
            <!-- Menu Items -->
            <ul class="flex items-center gap-1">
                <li>
                    <a href="/" class="px-4 py-2 rounded hover:bg-blue-700 transition <%= currentPath === '/' ? 'bg-blue-700' : '' %>">
                        خانه
                    </a>
                </li>
                <li>
                    <a href="/products" class="px-4 py-2 rounded hover:bg-blue-700 transition <%= currentPath.startsWith('/products') ? 'bg-blue-700' : '' %>">
                        محصولات
                    </a>
                </li>
                <li>
                    <a href="/categories" class="px-4 py-2 rounded hover:bg-blue-700 transition <%= currentPath.startsWith('/categories') ? 'bg-blue-700' : '' %>">
                        دسته‌بندی‌ها
                    </a>
                </li>
                <li>
                    <a href="/about" class="px-4 py-2 rounded hover:bg-blue-700 transition <%= currentPath === '/about' ? 'bg-blue-700' : '' %>">
                        درباره ما
                    </a>
                </li>
                <li>
                    <a href="/contact" class="px-4 py-2 rounded hover:bg-blue-700 transition <%= currentPath === '/contact' ? 'bg-blue-700' : '' %>">
                        تماس با ما
                    </a>
                </li>
            </ul>
            
            <!-- Search & Admin -->
            <div class="flex items-center gap-3">
                <!-- Search Form -->
                <form action="/products" method="GET" class="relative hidden md:block">
                    <input 
                        type="text" 
                        name="search" 
                        placeholder="جستجو در محصولات..." 
                        class="pl-10 pr-4 py-2 rounded-lg text-gray-800 w-64 focus:outline-none focus:ring-2 focus:ring-blue-300"
                    >
                    <button type="submit" class="absolute left-3 top-1/2 -translate-y-1/2 text-gray-500">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                        </svg>
                    </button>
                </form>
                
                <!-- Admin Link -->
                <% if (user) { %>
                    <a href="/admin/dashboard" class="px-4 py-2 bg-blue-700 rounded hover:bg-blue-800 transition">
                        پنل مدیریت
                    </a>
                <% } %>
            </div>
        </div>
    </div>
</nav>